<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@android:color/holo_blue_dark</item>
        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <item name="textColorPrimary">@android:color/white</item>
        <item name="windowBackground">@android:color/holo_blue_dark</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="colorAccent">@android:color/white</item>


        <item name="android:colorBackground">@android:color/holo_blue_dark</item>
        <item name="actionBarTheme">@android:color/holo_blue_dark</item>
        <item name="textColor">@android:color/black</item>
        <item name="colorButtonNormal">@android:color/darker_gray</item>
    </style>

        <TextView name="Text" parent="Theme.AppCompat.Light.DarkActionBar">
            <item name="TextSize">22dp</item>

        </TextView>
</resources>
